# AGENT INSTRUCTION: Copy to backend/.env and fill in real values. Never commit .env.
# Secret key for signing auth tokens. Generate with: python -c "import secrets; print(secrets.token_hex(32))"
WANDERLUST_SECRET_KEY=change-me-to-a-long-random-string
